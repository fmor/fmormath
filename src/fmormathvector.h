#pragma once

/*
namespace fmormath {

template< typename T>
struct fmVector
{
    virtual bool operator ==( const T& other ) const = 0;
    virtual bool operator !=( const T& other ) const = 0;

    virtual void operator *=( float f ) = 0;
    virtual void operator /=( float f ) = 0;
    virtual T operator *( float f ) const = 0;
    virtual T operator /( float f ) const  = 0;

    virtual void operator +=( const T& other ) = 0;
    virtual void operator -=( const T& other ) = 0;
    virtual T operator +( const T& other ) const  = 0;
    virtual T operator -( const T& other ) const  = 0;
    virtual T operator -() const = 0;


    virtual float dot( const T& other ) const = 0;
    virtual float length() const = 0;
    virtual float lengthSquared() const = 0;
    virtual void  scale( const T& other ) = 0;
    virtual void normalize() = 0;



};


}
*/
